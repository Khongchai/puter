Puter is a lightweight calculator syntax like `numi`, but right here in your editor.

Supports various types of operations. For more detail see the readme inside the client folder `/client/README.md`

# Development

Compile client => npm run compile in /client

Compile server => build-prod or debug in /server